def two_sum(nums, target):
    x = 0
    for i in range(0, len(nums)-1):
        for j in range(1, len(nums)):
            x = nums[i] + nums[j]
            if x == target:
                return ((i, j))
